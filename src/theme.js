import { createTheme } from "@mui/material";

const theme = createTheme({
    primary: {
        main: #ff556e
    }
    secondary: {
        main: #f7fafd,
        contrastText: #ff556e,
    }

})

export default theme